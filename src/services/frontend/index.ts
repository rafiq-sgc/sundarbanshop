/**
 * Frontend Services
 * Public-facing services that don't require authentication
 * Used for browsing products, viewing details, etc.
 */

export { frontendProductService } from './product.service'
export type { Product, ProductsResponse, ProductResponse, ProductFilters } from './product.service'

